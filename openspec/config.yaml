version: "1.0"
context: |
  Project: NefaHUB (Python)
  Stack: Pure Python, compilable via Cython (.c / .exe) and build.bat.
  Architecture: Custom structure, non-framework.
strict_tdd: false
testing_capabilities:
  test_runner: null
  unit: false
  integration: false
  e2e: false
  coverage: false
  linter: false
  type_checker: false
  formatter: false
phase_rules:
  proposal: "Focus on architectural boundaries before implementation."
  spec: "Keep specs concrete and executable."
  design: "Emphasize clean separation of concerns."
  tasks: "Break down into checkable atomic units."
  apply: "Write code matching the tasks checklist."
  verify: "Manual validation required since automated tests are missing."
  archive: "Store final diffs and learnings."
